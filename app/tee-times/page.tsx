import { TeeTimesView } from "@/components/TeeTimesView";

export const dynamic = "force-static";

export const metadata = {
  title: "Tee Times — Inshalla CC",
};

export default function TeeTimesPage() {
  return <TeeTimesView />;
}
